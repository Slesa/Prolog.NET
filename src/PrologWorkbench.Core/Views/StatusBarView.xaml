<UserControl x:Class="PrologWorkbench.Core.Views.StatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <ContentControl Background="{DynamicResource Brush.ContentControl.Background}">
        <StackPanel Margin="5" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="{DynamicResource Brush.ContentControl.Background}">
            <TextBlock Text="{Binding Message}" />
        </StackPanel>
    </ContentControl>
</UserControl>
